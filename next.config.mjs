/** @type {import('next').NextConfig} */
// const nextConfig = {};

const nextConfig = {
    reactStrictMode: true,
    images: {
      unoptimized: true, // Se precisar de suporte a imagens sem otimização
    },
  };
  
  export default nextConfig;
  

// module.exports = nextConfig;
